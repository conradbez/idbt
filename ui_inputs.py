
# User inputs
# if len(nodes)>0:
#     for node in nodes:

#         if diagram.selected['type'] == 'Select':
#             if diagram.selected['name'] == node.name:
#                 select_columns = st.multiselect("Select columns",options = column_options, default='*')
                    
#     nodes = add_user_config_to_nodes(nodes, st.session_state['node_user_config'])
